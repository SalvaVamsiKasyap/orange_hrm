pipeline {
    agent any

    stages {
        stage('Setup') {
            steps {
                // Clone the repository containing the pytest framework
                git 'https://github.com/SalvaVamsiKasyap/orange_hrm.git'
                sh 'pytest'
            }
        }

        
    }
}
